name: Android APK Build (SmsRupee)

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  build_apk:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # --- NEW STEP: FORCE CLEANUP OF BUILD ENVIRONMENT ---
      - name: Clean Up Local Caches
        run: |
          # Clears npm and Expo cache folders in the cloud runner environment
          rm -rf ~/.npm
          rm -rf ~/.expo

      # Set up Java 17, required for the native Android compilation
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      # Set up Node.js
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # Cache Node Modules: We keep the caching step for speed, but the cleanup above ensures a fresh start.
      - name: Cache Node Modules
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Install Dependencies
        run: npm install --force

      # Cache Gradle dependencies
      - name: Cache Gradle Dependencies
        uses: actions/cache@v4
        with:
          path: ~/.gradle/caches
          key: ${{ runner.os }}-gradle-${{ hashFiles('android/build.gradle') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      # --- Critical Step: Generates the Android/Gradle structure ---
      - name: Run Expo Prebuild (Generates Android/Gradle files)
        run: npx expo prebuild --platform android --no-install
        env:
          EXPO_NO_DOTENV: true 

      - name: Make Gradle Wrapper Executable
        run: chmod +x android/gradlew

      - name: Build Release APK (Includes Native Fixes)
        run: |
          # This compiles the custom Java files you created.
          cd android
          ./gradlew assembleRelease

      - name: Upload Release APK
        uses: actions/upload-artifact@v4
        with:
          name: smsrupee-release-apk
          path: android/app/build/outputs/apk/release/app-release.apk
          retention-days: 7
